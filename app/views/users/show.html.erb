<% provide(:title, @user.username) %>
<div class="row">
	<aside class="span4">
		<section>
			<h1>
				<%= gravatar_for @user, size: 100 %>
				<%= @user.username %>
			</h1>
		</section>
	</aside>
</div>